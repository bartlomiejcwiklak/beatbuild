export interface AlbumManifestEntry {
  id: string;
  title: string;
  folder: string;
  coverFront: string;
  coverBack: string;
  buttonMap: string;
  spine?: string;
  loops: string[];
}

export interface AlbumPreset {
  id: string;
  title: string;
  folder: string;
  coverFront: string;
  coverBack: string;
  buttonMap: string;
  spine?: string;
  loops: string[];
}

